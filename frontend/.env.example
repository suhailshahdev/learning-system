# Base URL for the backend API. Include /api prefix since all endpoints
# are mounted under /api in FastAPI. Frontend code uses relative paths
# like "/health" against this base.
VITE_API_BASE_URL=http://localhost:8000/api
